# Every ratified parameter, with the decision that ratified it.
#
# No value in this file may also appear as a literal in src/ or in a migration
# (FR-75, V65). The loader is the only reader. A missing key is a startup error,
# never a silent default.
#
# Values marked "arbitrated" are configurable because the evidence has not arrived
# yet, not because anyone may want to change them. Each names what decides it.

comparables:
  area_band_pct: 50          # D108
  recency_months: 12         # D110
  min_before_widening: 3     # D109 — a median of 3 is close to noise
  # O41 — the trigger above is ratified, the ladder is not. The last step
  # relaxes the area band D108 fixed at 50%, so it can return comparables
  # D108 excludes.
  widening_ladder:      # O41
    - gmina             # O41
    - radius_10km       # O41
    - radius_25km       # O41
    - powiat            # O41
    - powiat_area_band_100pct   # O41

aggregates:
  flow_window_days: 90       # D107
  iqr_switch_n: 5            # D67 — configuration, never a database CHECK

validation:
  area_min_m2: 300           # O12
  area_max_m2: 200000        # O12
  # V10 states the price band beside the area band. It was ratified in the same
  # breath and never reached this file, so it lived as a literal in the
  # normalizer — the one thing FR-75 exists to prevent.
  price_per_m2_min_pln: 1        # O12
  price_per_m2_max_pln: 100000   # O12
  conflict_threshold_pct: 5  # D81
  conflict_threshold_base: register   # D120 — the register area is the denominator

crawl:
  fraction_tolerance_pln: 1  # D94
  count_tolerance:           # D95 — the allowance is the larger of the two
    abs: 3                   # D95
    pct: 2                   # D95
  retry_after_max_s: 3600    # D93
  # The rate limit a source gets when it declares none. Slow on purpose.
  # Nobody ratified the number — see O42.
  default_rate_limit_rpm: 5  # O42

feasibility:
  good_neighbour_radius_m: 100      # D102 — arbitrated by the 20-parcel labelled set
  coverage_probe_radius_m: 500      # D103 — arbitrated by the same set
  coverage_probe_min_buildings: 3   # D103

surface:
  # Written as an escape: U+00A0 and a plain space look identical in a file.
  thousands_sep: "\u00A0"   # D125 — so a number never breaks across lines
